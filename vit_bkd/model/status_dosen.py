#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class status_dosen(models.Model):

    _name = "vit.status_dosen"
    _description = "vit.status_dosen"

    _inherit = "vit.status_dosen"


