<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <record model="ir.actions.act_window" id="action_account_jenis_asset">
            <field name="name">Jenis Aset</field>
            <field name="res_model">account.asset.category</field>
            <field name="domain">[('type', '=', 'purchase')]</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,kanban,form</field>
            <field name="context">{'default_type': 'purchase'}</field>
        </record>

        <!-- <record id="kelompok_aset_tag_action" model="ir.actions.act_window">
            <field name="name">Kelompok Aset</field>
            <field name="res_model">account.analytic.tag</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Add a new tag
              </p>
            </field>
        </record> -->

        <!-- <record id="action_res_users_milik" model="ir.actions.act_window">
            <field name="name">Pemilik Aset</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.users</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,kanban,form</field>
            <field name="view_id" ref="base.view_users_tree"/>
            <field name="search_view_id" ref="base.view_users_search"/>
            <field name="context">{'search_default_no_share': 1}</field>
            <field name="help">Create and manage users that will connect to the system. Users can be deactivated should there be a period of time during which they will/should not connect to the system. You can assign them groups in order to give them specific access to the applications they need to use in the system.</field>
        </record>
        <record id="action_res_users_view1" model="ir.actions.act_window.view">
            <field eval="10" name="sequence"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="base.view_users_tree"/>
            <field name="act_window_id" ref="action_res_users_milik"/>
        </record>
        <record id="action_res_users_view2" model="ir.actions.act_window.view">
            <field eval="20" name="sequence"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="base.view_users_form"/>
            <field name="act_window_id" ref="action_res_users_milik"/>
        </record> -->

        <record id="action_partner_form_milik" model="ir.actions.act_window">
            <field name="name">Pemilik Aset</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_type">form</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="search_view_id" ref="base.view_res_partner_filter"/>
            <field name="domain">[('pemilik_asset','=',True)]</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a new customer in your address book
              </p><p>
                Odoo helps you easily track all activities related to a customer.
              </p>
            </field>
        </record>
        <!-- <record id="action_partner_form_view_milik" model="ir.actions.act_window.view">
            <field eval="0" name="sequence"/>
            <field name="view_mode">kanban</field>
            <field name="view_id" ref="base.res_partner_kanban_view"/>
            <field name="act_window_id" ref="action_partner_form_milik"/>
        </record>
        <record id="action_partner_tree_view_milik" model="ir.actions.act_window.view">
            <field name="sequence" eval="1"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="base.view_partner_tree"/>
            <field name="act_window_id" ref="action_partner_form_milik"/>
        </record>
        <record id="action_partner_form_view_milik" model="ir.actions.act_window.view">
            <field eval="2" name="sequence"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="base.view_partner_form"/>
            <field name="act_window_id" ref="action_partner_form_milik"/>
        </record> -->

        <record id="product_uom_kateg_form_action" model="ir.actions.act_window">
            <field name="name">Kategori Satuan Aset</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">uom.category</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Add a new unit of measure category
              </p><p>
                Units of measure belonging to the same category can be
                converted between each others. For example, in the category
                <i>'Time'</i>, you will have the following units of measure:
                Hours, Days.
              </p>
            </field>
        </record>
        <record id="product_satuan_form_action" model="ir.actions.act_window">
            <field name="name">Satuan Aset</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">uom.uom</field>
            <field name="view_type">form</field>
            <field name="view_id" ref="uom.product_uom_tree_view"/>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Add a new unit of measure
              </p><p>
                You must define a conversion rate between several Units of
                Measure within the same category.
              </p>
            </field>
        </record>
        <!-- status aset -->
        <record model="ir.actions.act_window" id="status_aset_form_action">
            <field name="name">Status Aset</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.asset.asset</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="vit_asset.view_account_asset_asset_search"/>
            <field name="domain">[('asset_id','=',False),('category_id.parent_id.parent_id.parent_id','=','Aset')]</field>
            <field name="context">{'group_by':'state'}</field>
        </record>
    </data>
</odoo>