<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- tree view -->
        <record id="view_vit_mutasi_pengajuan_angka_kredit_tree" model="ir.ui.view">
            <field name="name">vit_mutasi_pengajuan_angka_kredit_tree</field>
            <field name="model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="type">tree</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <tree string="Mutasi pengajuan angka kredit">
                    <field name="name" />
                    <field name="state" />
                    <field name="matra_id" />
                    <field name="fakultas_id" />
                </tree>
            </field>
        </record>
        <!-- form view -->
        <record id="view_vit_mutasi_pengajuan_angka_kredit_form" model="ir.ui.view">
            <field name="name">vit_mutasi_pengajuan_angka_kredit_form</field>
            <field name="model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="type">form</field>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <form string="Mutasi pengajuan angka kredit">
                    <header>
                        <button string="Confirm" type="object" name="action_confirm" states="draft" />
                        <button string="Mark as Done" type="object" name="action_done" states="open" />
                        <button string="Reset to Draft" type="object" name="action_draft" states="open,done" />
                        <field name="state" widget="statusbar" />
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <!--button type="object" name="action_view_detail" class="oe_stat_button" icon="fa-pencil-square-o"-->
                                <!--field name="detail_count" widget="statinfo" string="Detail(s)"/-->
                                <!--field name="detail_ids" invisible="1"/-->
                            <!--/button-->
                        </div>
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only" string="Mutasi pengajuan angka kredit Name"/>
                            <h1><field name="name"/></h1>
                        </div>
                        <group>
                            <group>
                            </group>
                            <group>
                                <field name="matra_id" />
                                <field name="fakultas_id" />
                            </group>
                        </group>
                        <notebook>
                            <page string="Mutasi ak detail">
                                <field name="mutasi_ak_detail_ids"/>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>
        <!-- search -->
        <record id="view_vit_mutasi_pengajuan_angka_kredit_search" model="ir.ui.view">
            <field name="name">vit_mutasi_pengajuan_angka_kredit</field>
            <field name="model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="arch" type="xml">
                <search string="Mutasi pengajuan angka kredit">

                    <filter string="Name" name="filter_by_name" domain="[('name','=','')]" />
                    <filter string="State" name="filter_by_state" domain="[('state','=','')]" />
                    <field name="name" select="True"/>
                    <field name="state" select="True"/>
                    <field name="matra_id" select="True"/>
                    <field name="fakultas_id" select="True"/>
                    <group string="Group By..">
                        <filter string="Matra" domain="[]" name="group_by_matra_id" context="{'group_by':'matra_id'}"/>
                        <filter string="Fakultas" domain="[]" name="group_by_fakultas_id" context="{'group_by':'fakultas_id'}"/>
                    </group>
                </search>
            </field>
        </record>
        <!-- kanban , to show image, please add image_small in the model -->
        <record id="view_vit_mutasi_pengajuan_angka_kredit_kanban" model="ir.ui.view" >
            <field name="name">vit_mutasi_pengajuan_angka_kredit</field>
            <field name="model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="arch" type="xml">
                <kanban version="7.0">
                    <field name="id" />
                    <field name="name" />
                    <field name="state" />
                    <field name="matra_id" />
                    <field name="fakultas_id" />
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click o_kanban_record_has_image_fill">
                                <div class="oe_kanban_details">
                                    <h4>
                                        <a type="open"><field name="name"/></a>
                                    </h4>
                                    <ul>

                                        <li>
                                            <span class="text-muted">Matra</span> <field name="matra_id"></field>
                                        </li>


                                        <li>
                                            <span class="text-muted">Fakultas</span> <field name="fakultas_id"></field>
                                        </li>

                                    </ul>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
        <!-- calendar if a date field exists -->
        <!-- graph -->
        <record id="view_vit_mutasi_pengajuan_angka_kredit_graph" model="ir.ui.view">
            <field name="name">vit_mutasi_pengajuan_angka_kredit</field>
            <field name="model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="arch" type="xml">
                <graph string="Mutasi pengajuan angka kredit" type="bar">
                    <field name="matra_id"/>
                </graph>
            </field>
        </record>
        <!-- action window -->
        <record id="action_vit_mutasi_pengajuan_angka_kredit" model="ir.actions.act_window">
            <field name="name">Mutasi pengajuan angka kredit</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.mutasi_pengajuan_angka_kredit</field>
            <field name="view_mode">tree,form,graph</field>
            <field name="context">{"search_default_fieldname":1}</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Mutasi pengajuan angka kredit
                </p><p>
                Click the Create button to add a new Mutasi pengajuan angka kredit
                </p>
            </field>
        </record>

        <menuitem id="menu_vit_mutasi_pengajuan_angka_kredit" name="Mutasi pengajuan angka kredit" parent="vit_mutasi_sub_menu" action="action_vit_mutasi_pengajuan_angka_kredit" sequence="140"/>
    </data>
</odoo>