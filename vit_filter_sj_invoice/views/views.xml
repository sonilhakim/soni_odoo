<odoo>
    <data>
        <!-- SJ -->
        <record id="view_picking_form_flag" model="ir.ui.view">
            <field name="name">view_picking_form_flag</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form" />
            <field name="arch" type="xml">
                <field name="priority" position="after">
                    <field name="is_invoiced" readonly="1" attrs="{'invisible':[('picking_type_code','!=','outgoing')]}"/>
                </field>
            </field>
        </record>

    </data>
</odoo>