#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class nom_detail(models.Model):
    _name = "vit.nom_detail"
    _inherit = "vit.nom_detail"
