#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class detail_penilaian(models.Model):
    _name = "vit.detail_penilaian"
    _inherit = "vit.detail_penilaian"
