#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class to_user(models.Model):
    _name = "vit.to_user"
    _inherit = "vit.to_user"
