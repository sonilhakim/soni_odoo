#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class cc_user(models.Model):
    _name = "vit.cc_user"
    _inherit = "vit.cc_user"
