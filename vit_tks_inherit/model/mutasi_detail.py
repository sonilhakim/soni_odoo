#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class mutasi_detail(models.Model):
    _name = "vit.mutasi_detail"
    _inherit = "vit.mutasi_detail"
